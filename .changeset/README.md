# Changesets

Use `pnpm changeset` for every dispatcher change that should appear in release notes.
Run `pnpm run version` to apply pending changesets to `package.json` and `CHANGELOG.md`.

Dispatcher releases are Docker/GitHub releases from `dispatcher-v*` tags, not npm publishes.
